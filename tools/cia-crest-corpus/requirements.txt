# SPDX-FileCopyrightText: 2026 Knitli Inc.
# SPDX-License-Identifier: LicenseRef-MarqueLicense-1.0
#
# Runtime dependencies for the cia-crest-corpus pipeline.
#
# fetch_crest.py uses only the standard library (urllib + json + tomllib).
# extract_clean.py / cleaner.py likewise stdlib only.
# render_corpus.py and make_specs.py parse YAML frontmatter, so they need
# PyYAML.
#
# Pinned to an exact version so a `pip install -r requirements.txt` is
# reproducible.
PyYAML==6.0.2
