# Plan-of-Record: PR 3b Closeout — T027 / T028 / T029

**Branch**: `refactor-006-pr-3b-closeout` (worktree `/home/knitli/marque-pr3b-closeout/`, off `origin/staging` at `27095008` — PR 3b.F just merged)
**Base PR**: against `staging` (NOT `main`)
**Predecessors landed in `staging`**: PR 3b.A (#319 banner walker), PR 3b.B (#320 transmutations), PR 3b.C (#321 RELIDO conflicts), PR 3b.D (#324 class-floor catalog), PR 3b.E (#326 SCI per-system walker), PR 3b.F (#327 non-canonical-input walker)
**Status**: PM-APPROVED 2026-05-08 (OQs resolved with recommended defaults: Q-1 new file, Q-2 prefix-match, Q-3 both fresh table and sub-PR links, Q-4 no staging coverage, Q-5 skip Default==new tautology, Q-6 flip checkboxes not retire IDs, Q-7 startsWith confirmed valid)

---

## 0. One-paragraph summary

The PR-3b umbrella collapses 59 hand-written CAPCO rules into a declarative-catalog architecture across six functional sub-PRs (3b.A–3b.F), all of which have merged to `staging`. The closeout sub-PR delivers the three remaining bookkeeping tasks: **T027** (umbrella reviewer-attestation in the PR description: per-catalog single-§-citation discipline; ≤3 branches per `impl Rule`; net-rule-delta math from 59 → 47), **T028** (test-fixture consolidation gap-fill — chiefly an exact-rule-ID-set pin to complement the existing count pin in `corpus_parity.rs`), and **T029** (a CI matrix entry mirroring T025's PR-3a-only pattern, scoped to the umbrella + all six sub-PR branches + the closeout branch). Code surface is minimal: zero changes to `crates/{engine,scheme,core,rules,ism}` (Constitution VII §IV scheme-adoption restriction), zero rule-logic edits, one new integration-test file, one new CI matrix entry, three spec/agent-context updates (`tasks.md`, `plan.md`, `CLAUDE.md`).

---

## 1. Sub-PR scope

### 1.1 In scope

1. **T027 attestation work.** A PR-description block that aggregates the (a)/(b)/(c) attestation each sub-PR already declared, with an umbrella net-delta table running from pre-3b 59 through each sub-move's contribution down to post-3b.F 47.
2. **T028 consolidated regression test.** One new integration test (proposed: `crates/capco/tests/post_3b_registration_pin.rs`) that pins the **exact set** of 47 registered `Rule::id()` values, complementing the existing count pin at `corpus_parity.rs:170-194`. This catches a "swap one rule for another at the same count" drift that the count pin alone cannot.
3. **T029 CI matrix entry.** A new `pr-3b-corpus-regression` job in `.github/workflows/ci.yml`, slotted between `pr-3a-corpus-regression` (line 81–138) and `masking-pin-lint` (line 152), mirroring T025's body verbatim with a branch filter that covers the umbrella + all six sub-PR branches + the closeout branch.
4. **Spec-doc bookkeeping.** `tasks.md` flips T027 / T028 / T029 to `[X]`. `plan.md` D13 §"PR 3b sub-moves" gets a "PR-3b umbrella LANDED 2026-05-08 (sub-PRs #319 / #320 / #321 / #324 / #326 / #327; closeout #NNN)" annotation. `CLAUDE.md` "Recent Changes" gets a closeout entry.

### 1.2 Explicitly out of scope

- **The retired "13–18 numeric band."** D13 addendum (2026-05-07 amendment) retires the band; the gate is "stayed within the sub-move's authorized primitive scope," not a numeric target. Stage-1 expected count `~38–44` was descriptive, not normative; the actual 47 lands above the upper edge of `~38–44` by 3 rules, but the band was retired 2026-05-07 (D13 re-baseline) precisely because the literal sub-move retirements were known to land outside any 13-18 / 38-44 numeric band by construction.
- **Any rule-logic change.** Citations, severities, fix shapes, span behavior — all six sub-PRs already attested those individually. The closeout PR is bookkeeping; if the closeout reviewer surfaces a real logic defect during attestation, it returns to a separate sub-PR (don't backsmuggle fixes into closeout).
- **Engine-crate edits.** Constitution VII §IV: scheme-adoption PRs MUST NOT edit `marque-engine`, `marque-scheme`, `marque-core`, `marque-rules`, `marque-ism`. The closeout PR is bookkeeping atop scheme-adoption work and MUST observe the same discipline. No edits to those five crates.
- **Deprecation-phasing for retired rule IDs.** Per memory `feedback_pre_users_no_deprecation_phasing.md`, marque is pre-1.0 / pre-users; rewrite freely. The legacy E020/E022/E023/E025/E027/E028/E031/E033/E035/E040/E042–E051 IDs are NOT preserved as severity-config aliases. Walkers do, however, emit per-row `Diagnostic.rule = "E031"` / `"E035"` / `"E040"` for audit-stream continuity (3b.A) — that's by design and is documented in the existing `BannerMatchesProjectedRule` doc-comment at `crates/capco/src/rules.rs:5040–5061`.
- **Plan.md D13 / spec-text rewrites beyond marking T027/T028/T029 as DONE.** Don't extend the addendum prose. The umbrella landing annotation is one line per the existing 3b.D/3b.E/3b.F examples.

### 1.3 Why this is the final PR-3b sub-PR

PR-3b umbrella consists of six functional sub-moves (each its own `Rule`/`PageRewrite`/`Constraint::Custom` collapse) plus three closeout tasks (T027 attestation, T028 test consolidation, T029 CI). All six functional sub-moves have merged. T027/T028/T029 cannot land alongside any sub-move — they aggregate facts that only become true after the last sub-move merges (e.g., the running count math; the exact rule-ID set; the CI branch list). The closeout sub-PR is therefore the umbrella-completing commit; once it merges, PR 3b is done and the next umbrella (PR 3.7 / T108) can begin.

---

## 2. T027 — Registration attestation

### 2.1 Verify 47 registrations match the per-sub-move retirement claims

**Verification commands** (re-run pre-flight before opening the PR):

```bash
# Box::new entries inside CapcoRuleSet::new() body
awk '/^impl CapcoRuleSet \{/,/^impl RuleSet for CapcoRuleSet \{/' \
  crates/capco/src/rules.rs | grep -cE "Box::new\("
# Expected: 47

# Total impl Rule for blocks across the two rule files
grep -cE '^impl Rule for' \
  crates/capco/src/rules.rs crates/capco/src/rules_declarative.rs
# Expected: 28 + 19 = 47
```

**Confirmed inventory** (47 IDs):

`C001`, `E001`, `E002`, `E003`, `E004`, `E005`, `E006`, `E007`, `E008`, `E009`, `E010`, `E011`, `E012`, `E013`, `E014`, `E015`, `E016`, `E021`, `E024`, `E026`, `E029`, `E030`, `E031`, `E032`, `E036`, `E037`, `E038`, `E039`, `E041`, `E052`, `E053`, `E054`, `E055`, `E056`, `E057`, `E058`, `E059`, `E060`, `S001`, `S002`, `S003`, `S004`, `S005`, `S006`, `W002`, `W003`, `W034`.

There are no orphaned `impl Rule for` blocks; every `impl Rule for` is exactly one of the 47 registrations. The `BannerMatchesProjectedRule` walker has three per-row catalog entries at `rules.rs:5155 / 5166 / 5175` — `E031 / E035 / E040` — but those are diagnostic-emission tags, not `Rule::id()` values; they do not register a `Rule`.

### 2.2 Single CAPCO-§ citation per declarative catalog entry (D13 attestation a)

**Source-of-truth tour** (each catalog row has exactly one operative §-citation):

| Catalog | Location | Row count | Single-§ verification |
|---|---|---|---|
| `BANNER_CATEGORY_CATALOG` | `crates/capco/src/rules.rs:5155 / 5166 / 5175` (per-row) | 3 | E031: §H.5 p101 (SAR); E035: §H.4 per-system (SCI); E040: §H.9 p172 + p174 (NODIS+EXDIS — composite-cited single row in 3b.A plan because the §-pair names the same operative rule across two §H.9 templates) |
| `crate::scheme::CLASS_FLOOR_CATALOG` | `crates/capco/src/scheme.rs` | 27 | Each row carries its own §-citation per `marque-applied.md` §3.4.6 family granularity (catalog-pin: `class_floor_catalog.rs::catalog_declares_27_class_floor_rows`) |
| `crate::scheme::SCI_PER_SYSTEM_CATALOG` | `crates/capco/src/scheme.rs` | 5 | HCS-O comp §H.4 p64 / HCS-P NF §H.4 p66 / HCS-P sub §H.4 p68 / SI-G §H.4 p80 / TK NF §H.4 p87+p91+p95 (composite within the TK row only — same operative rule across three §H.4 templates) |
| RELIDO `Constraint::Conflicts` | `crates/capco/src/scheme.rs` (E054–E057 wrappers) | 4 | E054 §H.8 p154 / E055 §H.8 p154 / E056 §H.8 p136 / E057 §H.8 p140 |
| `NON_CANONICAL_CATALOG` | `crates/capco/src/rules_declarative.rs` (private, near `DeclarativeNonCanonicalInputRule`) | 5 | REL TO §H.8 p150-151 / JOINT §H.3 p56 / SIGMA §H.6 p108 / SAR §H.5 p99 / SCI §H.4 p61 |

**Composite-§-citation rows** (E040 banner-rollup, TK NF in SCI per-system) are D13-compliant per the per-sub-PR plans: a single row covers an operative rule that the manual repeats verbatim across multiple per-template §H sections; citing both anchors is a verifiability win, not a single-citation discipline violation. (Constitution VIII: "citation integrity is per-claim, not per-block".)

**Action items in T027 attestation**:
- Re-grep each cited page anchor against `crates/capco/docs/CAPCO-2016.md` (`begin page <N>` markers); commit the verification command transcript inside the PR description.
- Confirm no row carries a bare `§NN` form (FR-018) — the search regex is `\b§[A-Z]\.[0-9]+\b(?!\s*p)` (a §X.Y not followed by a page number).
- The `tools/citation-lint/` AST tool runs in CI and would already fail; the manual re-grep is the reviewer's evidence that they re-verified.

### 2.3 ≤3 branches per `impl Rule` (D13 attestation b)

The umbrella claim is: every walker's `check` body has ≤3 internal branches. Verification approach:

- **Walkers** (collapsed): `BannerMatchesProjectedRule` (3 branches: marking-type guard / page-context guard / dispatch loop — per `rules.rs:5087-5099`); `DeclarativeClassFloorRule` (3 layers per the 3b.D plan: axis-presence early-out / direct row dispatch / DRY emit); `DeclarativeSciPerSystemRule` (2 branches per the 3b.E plan: axis-presence early-out / catalog walk; `match row.kind` is 2 arms); `DeclarativeNonCanonicalInputRule` (2 branches per the 3b.F plan: axis-presence early-out / catalog walk); RELIDO declarative wrappers (each is a thin `Constraint::Conflicts` dispatcher, ≤3 by construction).
- **Hand-written rules** (non-collapsed, retained verbatim from pre-3b): each was already at ≤3 internal branches before PR-3b; this is not a 3b regression. The reviewer attests by spot-checking the five most complex retained rules: `MisorderedBlocksRule`, `SeparatorCountRule`, `JointUsaFirstRule`, `RelToOpaqueUncertainReductionSuggestRule`, `MissingNonUsPrefix`, and confirms each `evaluate`/`check` body has ≤3 outer `match`/`if` arms with nested `match`/`if` separately within budget.

If a retained rule violates ≤3, the closeout reviewer **flags it as a closeout-side finding** but does NOT refactor in this PR (Constitution VII §IV blocks substantive rule-body edits inside scheme-adoption-adjacent work; the fix would be a separate PR pre-3.7). Expect zero violations based on the 3b.A–3b.F plans, all of which traversed the same retained-rule list and found none.

### 2.4 Net-rule-delta math (D13 attestation c)

| Step | Sub-move | Rules retired | Rules added | Net delta | Running count |
|---|---|---|---|---|---|
| Pre-3b baseline (per consultation verdict 2026-05-07) | — | — | — | — | **59** |
| 3b.A (T026a) — banner-roll-up walker (#319) | E031 SAR + E035 SCI + E040 Non-IC dissem retired; `BannerMatchesProjectedRule` added | 3 | 1 | **−2** | 57 |
| 3b.B (T026b) — declarative `PageRewrite` roster (#320) | (data-only — declarative `PageRewrite` rows; no rules retired or added in this sub-PR per its own plan §1) | 0 | 0 | **0** | 57 |
| 3b.C (T026c) — RELIDO `Constraint::Conflicts` (#321) | 4 wrappers added (E054 / E055 / E056 / E057); none retired | 0 | 4 | **+4** | 61 |
| 3b.D (T026d) — class-floor catalog (#324) | E022 / E025 / E027 retired; `DeclarativeClassFloorRule` (E058) added | 3 | 1 | **−2** | 59 |
| 3b.E (T026e) — SCI per-system catalog (#326) | E042–E051 retired; `DeclarativeSciPerSystemRule` (E059) added | 10 | 1 | **−9** | 50 |
| 3b.F (T026f) — non-canonical input walker (#327) | E020 / E023 / E028 / E033 retired; `DeclarativeNonCanonicalInputRule` (E060) added | 4 | 1 | **−3** | **47** |

**Closeout terminal count: 47 registered rules.** End-state target ~10 surviving rules across all four stages remains binding (Stage 3 / PR 4 retires the banner walker + SCI per-system walker into per-category Lattice impls; Stage 4 / PR 5+ retires the non-canonical-input walker into the renderer).

### 2.5 Decision: does T027 require any code edits?

**Verdict: zero code edits.** The post-3b.F state in `staging` already satisfies (a)/(b)/(c). T027 is **pure PR-description attestation** — the reviewer drafts §2.1–2.4 above into the PR-3b umbrella PR description and re-verifies each claim against the merged tree.

If the closeout reviewer surfaces drift during pre-flight (a citation that wasn't carried forward across a sub-PR; a registration in unexpected order; a walker exceeding 3 branches because of an emergent edge case), each finding is enumerated as a **closeout-side gap** and either fixed in this PR (only if the fix is text-only — a citation typo, a comment correction) or punted to a follow-up PR (if logic-touching). The closeout PR's body documents the disposition.

---

## 3. T028 — Consolidated regression test

### 3.1 Per-sub-move test consolidation status

| Sub-move | Test deliverable | Live location | Status |
|---|---|---|---|
| 3b.A (banner equivalence) | `banner_rollup_walker.rs` + `banner_rollup_walker_overlap_guard.rs` | `crates/capco/tests/` | LANDED |
| 3b.B (per-`PageRewrite`) | `transmutation_rewrites.rs` + `phase_3_declares_nine_page_rewrites_with_citations` test in `corpus_parity.rs:198` + `phase_3_scheduler_exposes_nine_scheduled_rewrites` test in `corpus_parity.rs:233` | `crates/capco/tests/` | LANDED |
| 3b.C (per-RELIDO conflict) | `relido_conflicts.rs` (32 tests) | `crates/capco/tests/` | LANDED |
| 3b.D (per-class-floor row) | `class_floor_catalog.rs` (catalog-pin + per-row fires-once tests) | `crates/capco/tests/` | LANDED |
| 3b.E (per-SCI-per-system row) | `sci_per_system_catalog.rs` (5-row catalog-pin + per-row fires-once tests) | `crates/capco/tests/` | LANDED |
| 3b.F (per-non-canonical row) | `non_canonical_input_walker.rs` | `crates/capco/tests/` | LANDED |

**The per-sub-move work is complete.** What's MISSING at the umbrella level:

- A **count pin** exists at `corpus_parity.rs:170-194` and is current at 47 (with rationale text covering all six sub-moves).
- An **exact rule-ID set pin** does NOT exist. The count pin catches "rule registration was added/removed"; an exact-set pin catches "rule X was renamed to rule Y at the same count" or "rule X was deleted and rule Z was added at the same count." The latter drift is exactly what an umbrella regression test should catch — and it's the post-3b qualitative gate's load-bearing claim ("the closed set of 47 rules is the umbrella's commitment").

### 3.2 Candidate deliverable: exact-rule-ID-set pin

**Proposed file**: `crates/capco/tests/post_3b_registration_pin.rs` (new file).

**Test body** (sketch — implement only after PM-approval):

```rust
//! T028: post-PR-3b umbrella registration pin.
//!
//! Asserts the exact set of 47 registered Rule::id() values in
//! `CapcoRuleSet::new()`. This is the umbrella's structural commitment
//! — the per-sub-move tests pin per-walker behavior; this pins
//! "the closed set of 47 rules is what PR-3b umbrella delivered."
//!
//! Drift in this test is a CI failure. Bumping it requires intentional
//! review in PR 3.7 (T108) or later, not silent edit during routine work.

use marque_capco::CapcoRuleSet;
use marque_rules::RuleSet;
use std::collections::BTreeSet;

#[test]
fn post_3b_registers_exact_47_rule_ids() {
    let rule_set = CapcoRuleSet::new();
    let actual: BTreeSet<String> = rule_set
        .rules()
        .iter()
        .map(|r| r.id().as_str().to_owned())
        .collect();
    let expected: BTreeSet<&str> = [
        "C001",
        "E001", "E002", "E003", "E004", "E005", "E006", "E007", "E008", "E009",
        "E010", "E011", "E012", "E013", "E014", "E015", "E016",
        "E021", "E024", "E026", "E029", "E030", "E031", "E032", "E036",
        "E037", "E038", "E039", "E041",
        "E052", "E053", "E054", "E055", "E056", "E057", "E058", "E059", "E060",
        "S001", "S002", "S003", "S004", "S005", "S006",
        "W002", "W003", "W034",
    ]
    .into_iter().collect();
    assert_eq!(
        actual.len(), 47,
        "post-3b registered rule count drifted from 47: actual={actual:?}",
    );
    let missing: Vec<_> = expected.iter().filter(|id| !actual.contains(**id)).collect();
    let unexpected: Vec<_> = actual.iter().filter(|id| !expected.contains(id.as_str())).collect();
    assert!(
        missing.is_empty() && unexpected.is_empty(),
        "post-3b registered rule-ID set drifted. Missing (expected but not registered): {missing:?}. \
         Unexpected (registered but not expected): {unexpected:?}. \
         Bumping this test requires intentional review (PR 3.7 / T108 or later).",
    );
}
```

**Why a new file** (vs extending `rules_us1.rs` or `corpus_parity.rs`): isolation. The exact-set pin is a structural commitment, not a corpus-fixture parity test or a US-1 acceptance test. A new file makes the assertion's audience (umbrella reviewers, PR 3.7 authors) discoverable from the filename alone. It also avoids polluting `corpus_parity.rs`'s test list with a non-corpus assertion.

**Crate placement**: `crates/capco/tests/` — same as the existing count pin, same crate as `CapcoRuleSet`. **Constitution VII §IV-clean**: zero edits to `crates/{engine,scheme,core,rules,ism}`.

### 3.3 Orphan tests / fixtures from retired rules

Spot-check via:

```bash
grep -rE '"E0(20|22|23|25|27|28|31|33|35|40|4[2-9]|5[01])"|RuleId::new\("E0(20|22|23|25|27|28|31|33|35|40|4[2-9]|5[01])"\)' \
  crates/capco/tests/ tests/ 2>/dev/null
```

**Expected findings** (from pre-flight grep — all are intentional, not orphans):

- `relido_conflicts.rs:768-792`: comment block referencing E022/E025 as catalog supersession history. **Retain** — documentation of the 3b.D class-floor catalog absorption.
- `rel_to_invariants.rs:7-97`: doc-comment trace + tests covering the E020 → E060 lex-tiebreaker behavior change (FR-016 fixed-point convergence shifts from 1 pass to ≤2 passes). **Retain** — load-bearing test of the post-3b.F engine semantics.
- `banner_rollup_walker_overlap_guard.rs:8-139`: tests asserting E031 (the per-row catalog ID emitted by `BannerMatchesProjectedRule`) overlap-guard interactions with E028→E060 (the SAR row). **Retain** — these reference the live per-row diagnostic IDs, not retired registered rules.

**No orphan fixtures expected**. Each sub-PR's reviewer dispatched its own test reshape; the 3b.A / 3b.D / 3b.E / 3b.F plans explicitly enumerated test deletions. T028's gap is the umbrella-level set pin (§3.2), not orphan removal.

If the closeout reviewer surfaces an orphan during pre-flight (a `.expected.json` fixture referencing a retired rule ID, an `assert_eq!` against a literal `RuleId::new("E020")` outside the documented retain-list), **delete** the fixture/assert in the closeout PR with a 1-line justification per deletion in the PR body.

### 3.4 Spec-doc updates

| Doc | Change | Pattern reference |
|---|---|---|
| `specs/006-engine-rule-refactor/tasks.md:108-110` | T027 / T028 / T029 checkbox `[ ]` → `[X]` | Same as 3b.D #324 / 3b.E #326 / 3b.F #327 |
| `specs/006-engine-rule-refactor/plan.md:266-340` | Append a "**3b umbrella — LANDED 2026-05-08** (closeout sub-PR #NNN aggregating 3b.A–F)" annotation at the end of the sub-move list | Same as the existing 3b.D / 3b.E "LANDED 2026-05-08" lines |
| `CLAUDE.md:268` "Recent Changes" | Prepend a closeout entry: T027 attestation (citation + ≤3-branch + net-delta math); T028 exact-rule-ID-set pin landed at `crates/capco/tests/post_3b_registration_pin.rs`; T029 CI matrix entry; final registered count 47 (within Stage-1 descriptive band; D13 numeric band retired 2026-05-07) | Same prose pattern as the existing 3b.E / 3b.F entries |
| `CLAUDE.md:247` "Current Status" | Update "47 registered CAPCO rules post-PR-3b.F" → "47 registered CAPCO rules post-PR-3b (umbrella complete)" | Cosmetic but intentional — signals the umbrella state to the next session's agents |

---

## 4. T029 — CI matrix entry

### 4.1 Branch filter

Mirror T025's `if:` form. T025 uses:

```yaml
if: github.ref == 'refs/heads/refactor-006-pr-3a' || github.head_ref == 'refactor-006-pr-3a'
```

T029 must cover the umbrella branch + all six sub-PR branches + the closeout branch. Simplest filter:

```yaml
if: |
  startsWith(github.ref, 'refs/heads/refactor-006-pr-3b') ||
  startsWith(github.head_ref, 'refactor-006-pr-3b')
```

This catches:
- `refactor-006-pr-3b` (umbrella, if it exists as its own branch)
- `refactor-006-pr-3b-banner-walker`
- `refactor-006-pr-3b-transmutations`
- `refactor-006-pr-3b-conflicts`
- `refactor-006-pr-3b-class-floor`
- `refactor-006-pr-3b-sci-per-system`
- `refactor-006-pr-3b-non-canonical-input`
- `refactor-006-pr-3b-closeout` (this PR)
- `refactor-006-pr-3b-rule-count-reframe`

**Why prefix-match over enumerated equality**: enumerated form is more precise but more brittle — it requires editing every sub-PR-branch name into ci.yml, which is exactly the sort of bookkeeping that drifts. The prefix `refactor-006-pr-3b` is namespace-clean per the project's branch-naming convention. The umbrella naming is convention, not hard-enforced — if someone names a follow-up branch `refactor-006-pr-3b-followup-thing`, we'd want this CI job to run, which validates the prefix-match approach.

### 4.2 Job body

Identical to T025's body (lines 86–138):
- Three corpus suites in three packages: `cargo test -p marque-capco --test corpus_parity` / `-p marque-engine --test corpus_accuracy` / `-p marque --test corpus_provenance`, each with `--features marque-engine/corpus-override`.
- Phase 4 gated suites step: `cargo test -p marque-engine -p marque --features marque-engine/decoder-harness,marque-engine/corpus-override,marque/corpus-override`.
- Same toolchain pin (`nightly` for `dtolnay/rust-toolchain`).
- Same cache config.

### 4.3 Slot location

Insert as a new job between line 138 (end of `pr-3a-corpus-regression`) and line 140 (comment block before `masking-pin-lint`). Verified line numbers via `wc -l ci.yml = 606` total; insertion at line 139 expands the file by ~55 lines (T025's job is ~55 lines).

The comment block above the new job mirrors T025's "Refactor-006 PR-3a-only corpus regression sweep" intro paragraph — but cites SC-014 + the umbrella subsequence semantics: "T029 — corpus regression sweep × {3a + 3b} subsequence. T025 (the PR-3a-only sweep) validates 3a in isolation; T029 validates the 3a+3b subsequence by running the same corpus suites on every 3b-prefixed branch (umbrella + sub-PRs + closeout)."

### 4.4 SC-014 interpretation: one job or two?

**SC-014 says**: "Each PR in the keystone subsequence (3a, 3b, 3c) and PR 6 sub-commit sequence (6a, 6b, 6c) passes the corpus regression sweep independently in the CI matrix; any single PR is mechanically revertable without leaving orphaned types, functions, or dependencies."

**Reading**: "each PR" here means each keystone PR — T025 is the 3a-only sweep, T029 is the 3b-and-after sweep, and a future T030-equivalent will be the 3c-only sweep. The "subsequence" framing in tasks.md ("`{3a + 3b} = 1 run validating the subsequence is correct`") confirms it: T029 is **one** new job that validates the post-3b state on 3b-prefixed branches; the post-3a state already has its own job at T025. Total post-this-PR jobs: T025 (3a-only), T029 (3b-and-after; covers 3b's six sub-PRs + closeout). That mirrors the spec wording and avoids redundant runs.

Verification reference: tasks.md:110 reads `{3a + 3b} = 1 run` — the "1 run" disambiguates: one CI job, not two. T029 is the 3b-and-after sweep; T025 stays unchanged.

---

## 5. Acceptance criteria

1. **47 registered rules verified** by `awk` count + `grep -c '^impl Rule for'` count + the new exact-set pin test.
2. **All 47 citations re-verified** against `crates/capco/docs/CAPCO-2016.md` page anchors via grep transcript in PR description (no fabricated, drifted, or out-of-normative-range citations remain).
3. **`tools/citation-lint/`** passes (existing CI gate; this PR does not regress it).
4. **Catalog-pin test** at `crates/capco/tests/post_3b_registration_pin.rs` lands and passes; the existing count pin at `corpus_parity.rs:170-194` passes unchanged.
5. **New CI job** runs on every 3b-prefixed branch + the closeout branch and passes (corpus_parity, corpus_accuracy, corpus_provenance, Phase 4 gated suites all green).
6. **`tasks.md` / `plan.md` / `CLAUDE.md`** updated per §3.4.
7. **T027 attestations (a/b/c)** drafted in PR description with verification grep transcripts.
8. **Pre-flight green** locally before opening PR: `cargo +stable check --workspace`, `cargo +stable clippy --workspace --all-targets -- -D warnings`, `cargo +stable fmt --check`, `cargo nextest run --workspace --profile ci`.
9. **Bench-gate green**: `bash scripts/bench-check.sh` (lint_10kb / decoder_10kb_one_mangled_region / lint_scaling / deadline_overhead within baseline+10%).
10. **Constitution-clean**: zero edits to `crates/{engine,scheme,core,rules,ism}` (Constitution VII §IV); no new `__engine_promote` call sites (Constitution V Principle V); no bare `§NN` citations (FR-018); no fabricated authority strings (Constitution VIII).
11. **Commits GPG-signed**, never `--no-gpg-sign`, never `--no-verify`.
12. **PR description** carries fresh net-delta math for the umbrella (table from §2.4) AND points back at each sub-PR description for sub-move-level detail (per OQ-3 below).

---

## 6. Open questions

**(Q-1) Should the exact-rule-ID-set pin live in a new file or extend `rules_us1.rs`?**
Rec: new file (§3.2 rationale — discoverability from filename alone). PM may prefer extending `rules_us1.rs` to avoid expanding the test-file inventory; both options are Constitution-clean. Decision is stylistic, not load-bearing.

**(Q-2) Branch filter: prefix-match or enumerated equality?**
Rec: prefix-match (`startsWith(github.ref, 'refs/heads/refactor-006-pr-3b')`) per §4.1 rationale. Risk: a future unrelated branch named `refactor-006-pr-3b-something` would trigger the corpus job. Mitigation: project naming convention is binding. Alternative: enumerate explicitly (more brittle but precise). PM call.

**(Q-3) PR description net-delta math: aggregate fresh, or point at each sub-PR's description?**
Rec: BOTH — embed §2.4's table fresh in the umbrella PR description (the umbrella reviewer needs it inline; chasing six sub-PR descriptions is expensive), AND link out to each sub-PR for the sub-move-level rationale. The fresh aggregation IS the umbrella attestation; the sub-PR links are the audit trail.

**(Q-4) `staging`-branch coverage on the new CI job?**
Rec: NO — match T025's PR-branch-only pattern. Post-merge revertability is exercised by `main`/`staging` push protection separately (existing). Adding `refs/heads/staging` to the filter would run the corpus job on every staging push (cost without signal post-merge).

**(Q-5) Does T028 need a "rule-set is shape-stable across `CapcoRuleSet::default()` and `CapcoRuleSet::new()`" assertion?**
Probably no — `Default::default()` calls `Self::new()` directly (`crates/capco/src/rules.rs:96`), so the two are byte-identical by construction. Adding the assertion is a no-op tautology. Skip unless reviewer disagrees.

**(Q-6) Should the closeout PR retire the literal "T027 / T028 / T029" task IDs from the plan body, or just check them off?**
Rec: just check them off, don't retire (other tasks/agents reference T027/T028/T029 by ID; retiring breaks cross-references). PM call if there's a project preference for "retire-not-strikethrough" on completed tasks.

**(Q-7) Is `prefix-match` filter syntax (`startsWith(github.ref, ...)`) supported in the existing GitHub Actions `if:` expression syntax?**
YES — `startsWith` is a built-in expression function. T025's existing equality form is also supported. No syntactic risk.

**Total open questions: 7.** None are blocking; all are stylistic / scope-confirmation items the PM can resolve in plan-review without further exploration.

---

## 7. Workflow timeline

1. **Pre-flight inventory (this plan)** — DONE pending PM approval. The inventory is the load-bearing pre-condition: 47 registered rules, 47 `impl Rule for` blocks, 47 unique `Rule::id()` strings.
2. **PM-approval gate (next)** — PM reviews this plan, resolves §6 open questions (especially Q-1 file location, Q-2 branch filter form, Q-3 fresh-vs-link description), gives approval signal.
3. **Implementation (separate agent)** — author the closeout PR per the approved plan: PR description (T027 attestation), `crates/capco/tests/post_3b_registration_pin.rs` (T028), `.github/workflows/ci.yml` job (T029), `tasks.md` / `plan.md` / `CLAUDE.md` updates. Pre-flight `cargo +stable clippy --workspace --all-targets -- -D warnings` + fmt + nextest + bench-check before commit. **GPG-sign every commit; never `--no-verify`.**
4. **Pre-PR-open reviewer dispatch** — dispatch `rust-reviewer` and `code-reviewer` agents in parallel against the closeout-branch HEAD. Reviewers attest: (a) Constitution VII §IV no-engine-edits, (b) Constitution V Principle V no-new-`__engine_promote`-callers, (c) Constitution VIII citation fidelity, (d) the new pin test catches the targeted drift class (rename-at-same-count + delete-and-add-at-same-count), (e) the new CI job branch filter actually fires on the closeout branch (verifiable by checking the existing PR's CI run-list).
5. **PR open** against `staging` (NOT `main`). Title pattern: `refactor-006 PR 3b closeout: T027/T028/T029 attestation + CI matrix + registration pin`. Body carries §2.4 net-delta table + §6 open-question disposition.
6. **Monitor** CI (the new job will be the load-bearing run on this PR's branch — confirm green) + Copilot review.
7. **Iterate to merge**. Squash-merge with the standard ECC `Co-Authored-By:` trailer.

---

## Critical files for implementation

- `/home/knitli/marque-pr3b-closeout/.github/workflows/ci.yml` (insert new job at line 139, between `pr-3a-corpus-regression` and `masking-pin-lint`)
- `/home/knitli/marque-pr3b-closeout/crates/capco/tests/post_3b_registration_pin.rs` (NEW — exact-rule-ID-set pin)
- `/home/knitli/marque-pr3b-closeout/specs/006-engine-rule-refactor/tasks.md` (T027/T028/T029 checkbox flips at lines 108-110)
- `/home/knitli/marque-pr3b-closeout/specs/006-engine-rule-refactor/plan.md` (umbrella LANDED annotation in the §"PR 3b sub-moves" block at lines 266-340)
- `/home/knitli/marque-pr3b-closeout/CLAUDE.md` (Recent Changes prepend at line 268; Current Status update at line 247)
